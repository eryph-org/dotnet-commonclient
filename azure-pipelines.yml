resources:
  repositories:
  - repository: build
    type: github
    name: Haipa/build
    endpoint: haipa


extends:
  template: pipelines/templates/dotnetclient@build
  parameters:
    withCmdlet: true